# dependencies
node_modules/

# build outputs
dist/
.astro/

# env
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor files
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# local preview / temporary files
*.log
*.tmp
*.temp
~$*

# local-only work archives
archive/original_uploads/
ブログの種（使用済）/
design/reference/

# local-only source materials
articles/sources/
project/

# visual-review internal logs (screenshots + review .md) — never push to main
logs/visual-review/

# automation runtime state (queue / locks / processed URL ledger) — never committed
data/automation/autonomy.json
data/automation/ledger.json
data/automation/scout-picked.json
data/automation/sumahon-queue.json
data/automation/processed-urls.json
data/automation/locks/

# night-run runtime audit state — private/local only
logs/night/last-run.json
logs/night/run-history.jsonl
logs/night/run-contract/
logs/night/*.completion.json
logs/night/*.heartbeat.json
logs/night/*.watchdog.json
logs/night/auth-probe/
logs/night/runner-package-lock.sha256
logs/night/scheduled-acceptance.request.json
logs/night/scheduled-acceptance.result.json

# automation logs (per-run files) — never committed; .gitkeep stays
logs/automation/*.log
logs/automation/*-sumahon-watch.json
logs/automation/scheduler-empty-gitconfig

# L1: last-good build snapshot (rollback fallback material) — local only
builds/

# L1: post-publish verify logs (Actions uploads them as artifacts)
logs/publish/

